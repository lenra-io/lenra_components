import 'package:flutter/widgets.dart';

class LenraSliderStyle {
  final Color? activeColor;
  final Color? inactiveColor;
  final Color? thumbColor;

  LenraSliderStyle({
    this.activeColor,
    this.inactiveColor,
    this.thumbColor,
  });
}