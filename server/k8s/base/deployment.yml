kind: Deployment
apiVersion: apps/v1
metadata:
  name: lenra-server
  labels:
    k8s-app: lenra-server
spec:
  replicas: 1
  selector:
    matchLabels:
      k8s-app: lenra-server
  template:
    metadata:
      name: lenra-server
      labels:
        k8s-app: lenra-server
    spec:
      securityContext:
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: lenra-server
          image: "registry.gitlab.com/lenra/platform/lenra_core/server"
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 4000
            - name: epmd
              containerPort: 4369
          env:
            - name: PORT
              value: "4000"
            - name: SERVICE_NAME
              value: lenra-server-headless.lenra-test.svc.cluster.local
            - name: HOSTNAME
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
          envFrom:
            - secretRef:
                name: lenra-server
            - secretRef:
                name: openfaas
            - secretRef:
                name: lenra-postgres
          securityContext:
            privileged: false
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 1
            failureThreshold: 10
            successThreshold: 1
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 60
            periodSeconds: 20
            timeoutSeconds: 1
            failureThreshold: 2
            successThreshold: 1
          resources:
            requests:
              cpu: "100m"
              memory: "100Mi"
            limits:
              cpu: "500m"
              memory: "500Mi"
      restartPolicy: Always
      imagePullSecrets:
        - name: gitlab-registry
  revisionHistoryLimit: 2
  progressDeadlineSeconds: 60
